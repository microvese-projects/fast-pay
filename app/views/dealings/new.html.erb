<div class='header'>
  <%= link_to user_group_path(current_user, params[:group_id]), class: 'back-btn' do %>
    <i class="fas fa-arrow-left"></i>
  <% end%>
  <h2 class='heading-form'>New Dealing</h2>
  <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: 'sign-out' %>
</div>

<%= render "form", dealing: @dealing %>
